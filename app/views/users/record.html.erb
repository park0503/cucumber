<h1>Order Record</h1>
<h3>Shipping Orders</h3>
<div class = "orders">
    <% if  @shippings.present?%>
        <% @shippings.each do |order| %>
            <div class = "order">
                <% order.line_items.each do |line_item| %>
                    <% item = line_item.item %>
                    <div class = "order__line_items">
                        <%= render line_item, item: item, line_item: line_item %>
                    </div>
                <% end %>
                <div class="order__amount">
                    총 결제 금액 : <%= order.amount %>원
                </div>
            </div>
        <% end %>
    <% else %>
        배송중인 상품이 없습니다.
    <% end %>
</div>

<h3>Arriving Orders</h3>
<div class = "orders">
    <% if  @arrivings.present?%>
        <% @arrivings.each do |order| %>
            <div class = "order">
                <% order.line_items.each do |line_item| %>
                    <% item = line_item.item %>
                    <div class = "order__line_items">
                        <%= render line_item, item: item, line_item: line_item %>
                    </div>
                <% end %>
                <div class="order__amount">
                    총 결제 금액 : <%= order.amount %>원
                </div>
            </div>
        <% end %>
    <% else %>
        배송 완료된 상품이 없습니다.
    <% end %>
</div>